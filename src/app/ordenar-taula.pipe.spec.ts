import { OrdenarTaulaPipe } from './ordenar-taula.pipe';

describe('OrdenarTaulaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarTaulaPipe();
    expect(pipe).toBeTruthy();
  });
});
